var fs = require('fs');
var xml3js = require('xml3js');

var parser = new xml3js.Parser();
fs.readFile(__dirname + '../critikat.xml', function(err, data) {
    parser.parseString(data, function (err, result) {
        console.dir(result);
        console.log('Done');
    });
});